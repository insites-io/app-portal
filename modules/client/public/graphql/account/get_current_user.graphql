query get_current_user {
  current_user {
    id
    first_name
    last_name
    name
    email
    external_id
    ## INS Contact Number
    # insites_contact_number: profiles(
    #   profile_type: "modules/insites_core/insites_contact_number"
    # ) {
    #   mobile_phone_country_code: property(name: "mobile_phone_country_code")
    #   mobile_phone_area_code: property(name: "mobile_phone_area_code")
    #   mobile_phone_number: property(name: "mobile_phone_number")
    # }
    
    ## INS Customer 
    # insites_customer: profiles(
    #   profile_type: "modules/insites_core/insites_customer"
    # ) {
    #   is_enabled: property_boolean(name: "is_enabled")
    #   company_id: property(name: "company_id")
    # }
    
    ## client
    client: profiles(
      profile_type: "modules/client/client"
    ) {
      is_enabled: property_boolean(name: "is_enabled")
      is_activated: property_boolean(name: "is_activated")
      company_uuid: property(name: "company_uuid")
    }

    # profiles{
    #   profile_type
    #   properties
    # }
  }
}