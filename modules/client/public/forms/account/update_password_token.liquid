---
name: update_password_token
resource: User
resource_owner: anyone
fields:
  email:
  profiles:
    modules/insites_core/token:
      properties:
        password_token:
          validation: { presence: true }
email_notifications:
  - modules/client/send_recover_password
---
  {%- log 'update_password_token', type: 'Test form:' -%}