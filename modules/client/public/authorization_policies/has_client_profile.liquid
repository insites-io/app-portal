---
name: has_client_profile
---
{%  liquid
    graphql data = 'modules/client/account/get_current_user'

    session user_uuid = data.current_user.external_id

    if data.current_user.client == blank
        graphql logout = 'modules/client/session/delete_current_session'
        echo true    
    else 
        echo true    
    endif
%}