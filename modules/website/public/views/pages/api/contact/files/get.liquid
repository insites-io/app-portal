---
slug: api/attachments/s3-form
method: get
searchable: false
---

{%- assign model_schema_name = context.params.name | default: null -%}
{%- assign property_name = context.params.field | default: null -%}

{%- graphql results, model_schema_name: model_schema_name, property_name: property_name -%}
mutation get_s3_credentials($model_schema_name: String!, $property_name: String!) {
  s3_upload: property_upload_presigned_url(
    model_schema_name: $model_schema_name
    property_name: $property_name
  ) {
    direct_upload_url: upload_url
    form_data: upload_url_payload
  }
}
{%- endgraphql -%}

{
  "{{ property_name }}": {{ results }}
}