---
metadata: {
    partial_name: 'Website | Forms | Update document files'
}
---

{%- assign id = id -%}
{%- assign document = document | replace: '["', "" -%}
{%- assign document = document | replace: '"]', "" -%}
{%- assign document = document | split: "," -%}

{%- graphql result = 'modules/website/forms/update_document_value', id: id, document: document -%}