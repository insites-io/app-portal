mutation set_profile_to_invited_users(
  $id: ID!
  $token: String!
) {
  user_update(
    id: $id
    user: {
      profiles: [
        {
          name: "modules/portal/portal",
          values: {
            properties: [
              { name: "is_enabled", value_boolean: true}
              { name: "is_activated", value_boolean: true}
              { name: "verification_token", value: $token }
            ]
          }
        }
      ]
    }
    form_name: "modules/portal/set_profile_details_to_invited_users"
  ) {
    id
  }
}