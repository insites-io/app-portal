---
name: add_contact_address
to: 'https://{{context.location.host}}/crm/api/v2/contacts/addresses'
format: http
request_type: POST
request_headers: >
    {% include 'modules/portal/api_headers' %}
---
{%  liquid     
    assign props = data.properties

    ## Ecommerce module passes contact_uuid rather than relying on the logged-in user's UUID because guest checkout also saves CRM data.
    if props.contact_uuid != ''
        assign contact_uuid = props.contact_uuid
    else 
        graphql user = 'modules/portal/account/get_user_uuid', id: data.id | dig: 'items','results' | first
        assign contact_uuid = user.uuid
    endif
%}
{
    "contact.uuid": "{{ contact_uuid }}",
    "address_label": "{{ props.address_1 | default: 'Default' }}", 
    "default_address": true,
    "address_1": "{{ props.address_1 }}",
    "address_2": "{{ props.address_2 }}",
    "suburb": "{{ props.suburb }}",
    "state": "{{ props.state }}",
    "postcode": "{{ props.postcode }}",
    "country": "{{ props.country }}",
    "latitude": "{{ props.latitude }}",
    "longitude": "{{ props.longitude }}"
}